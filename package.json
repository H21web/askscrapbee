{
  "name": "iask-scraper",
  "version": "1.0.0",
  "description": "Dynamic scraper for iask.ai",
  "main": "api/scrape.js",
  "scripts": {
    "postinstall": "npx puppeteer browsers install chrome"
  },
  "dependencies": {
    "puppeteer": "^24.16.2"
  },
  "engines": {
    "node": "22.x"
  }
}
