{
  "name": "iask-scraper",
  "version": "1.0.0",
  "description": "Dynamic scraper for iask.ai",
  "main": "api/scrape.js",
  "scripts": {
    "postinstall": "npx playwright install chromium --with-deps"
  },
  "dependencies": {
    "playwright": "^1.40.0"
  },
  "engines": {
    "node": "22.x"
  }
}
